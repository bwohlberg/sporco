language: python

python:
  - "2.7"
  - "3.4"
  - "3.5"

notifications:
  email: false

addons:
  apt:
    packages:
    - libfftw3-dev
    - libatlas-dev
    - libatlas-base-dev
    - liblapack-dev
    - libblas-dev
    - gfortran

before_install:
    - wget http://repo.continuum.io/miniconda/Miniconda-latest-Linux-x86_64.sh -O miniconda.sh
    - chmod +x miniconda.sh
    - ./miniconda.sh -b
    - export PATH=/home/travis/miniconda2/bin:$PATH
    - conda update --yes conda

install:
    - conda install --yes python=$TRAVIS_PYTHON_VERSION
    - conda install --yes -c https://conda.binstar.org/richli pyfftw
    - conda install --yes python=$TRAVIS_PYTHON_VERSION future numpy scipy matplotlib
    - python setup.py install

script:
    - python setup.py test

